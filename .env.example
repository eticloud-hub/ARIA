# ARIA Infrastructure Environment Variables

# PostgreSQL Database
POSTGRES_DB=aria
POSTGRES_USER=aria_admin
POSTGRES_PASSWORD=<your-secure-password-here>
# Note: The backend application uses these distinct environment variables in production:
# DATABASE_URL=postgresql://aria_admin:aria_dev_password@localhost:6432/aria
# DIRECT_DATABASE_URL=postgresql://aria_admin:aria_dev_password@localhost:5433/aria
