[project]
name = "aria-worker"
version = "1.0.0"
description = "ARIA HABD Analysis Worker — Python/Celery"
requires-python = ">=3.12"

[tool.setuptools]
packages = ["aria_worker"]

[project.optional-dependencies]
dev = ["pytest>=8.0", "pytest-cov", "ruff", "mypy"]
